# Local dev — Phase 9 API on your machine
PULSE_API_URL=http://localhost:8000

# Optional legacy alias (server fetches prefer PULSE_API_URL)
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (Vercel) defaults to https://groww-pulse-api.onrender.com via vercel.json + config.ts
